<mvc:View 
    controllerName="com.perezjquim.energysim.client.controller.Cars"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:mvc="sap.ui.core.mvc">
    <Page 
    	title="{i18n>Cars}">
        <Table 
        	items="{ws_data>/cars}">
            <columns>
                <Column>
                    <Text 
                    	text="{i18n>car_id}" />
                </Column>
                <Column>
                    <Text 
                    	text="{i18n>car_status}" />
                </Column>
                <Column>
                    <Text 
                    	text="{i18n>car_battery_level}" />
                </Column>
                <Column>
                    <Text 
                    	text="{i18n>car_plug_consumption}" />
                </Column>
            </columns>
            <items>
                <ColumnListItem>
                    <cells>
                        <Text 
                        	text="{ws_data>id}"/>
                        <ObjectStatus
				            text="{ parts: [ 'ws_data>is_charging', 'ws_data>is_traveling' ], formatter: '.formatCarStatusText' }"
				            icon="{ parts: [ 'ws_data>is_charging', 'ws_data>is_traveling' ], formatter: '.formatCarStatusIcon' }"
				            state="{ parts: [ 'ws_data>is_charging', 'ws_data>is_traveling' ], formatter: '.formatCarStatus' }" />
                        <ProgressIndicator
                            percentValue="{= ( ${ws_data>battery_level} / 10 ) * 100 }"
                            displayValue="{ parts: [ 'ws_data>battery_level' ], formatter: '.formatBatteryLevelText' }"
                            state="{ parts: [ 'ws_data>battery_level' ], formatter: '.formatBatteryLevelState' }"
                            showValue="true" />
                        <Text 
                        	text="{ws_data>plug_consumption}"/>
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Page>
</mvc:View>